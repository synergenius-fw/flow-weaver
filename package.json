{
  "name": "@synergenius/flow-weaver-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm -ws --if-present run typecheck",
    "lint": "npm -ws --if-present run lint",
    "test": "npm -ws --if-present run test"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
